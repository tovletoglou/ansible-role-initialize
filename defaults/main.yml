---

# The SELinux mode (enforcing | permissive | disabled).
initialize_selinux: disabled

# Packages installed with yum.
initialize_packages:
  - curl
  - gcc
  - kernel-devel
  - libzip
  - man
  - nano
  - net-tools
  - nmap
  - postfix
  - tree
  - unzip
  - vim
  - wget
  - zip

# Update all packages to the latest version.
initialize_packages_update: yes
